# Markdown puro

Este directorio contiene documentación en Markdown sin tooling de compilación.

- No scripts
- No templates LaTeX
- No assets de render

Si necesitás generar PDF académico, usá `docs/latex/`.
