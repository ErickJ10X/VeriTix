% ============================================================
%  VeriTix — Plantilla LaTeX estilo FOC
%  Convenciones: documentación académica/técnica
%  Motor: XeLaTeX  |  Pandoc template
% ============================================================

% ── Clase base ──────────────────────────────────────────────
\documentclass[
  $if(fontsize)$$fontsize$,$endif$
  $if(papersize)$$papersize$paper,$endif$
  twoside=false,
  parskip=half               % espacio entre párrafos sin sangría (estilo técnico)
]{$if(documentclass)$$documentclass$$else$scrreprt$endif$}                  % KOMA-Script: mejor base para docs académicos que 'report'

% ── Codificación y tipografía ───────────────────────────────
\usepackage{fontspec}        % fuentes OpenType/TrueType con XeLaTeX
\usepackage{polyglossia}
\setdefaultlanguage[variant=spanish]{spanish}
\frenchspacing               % espaciado uniforme tras punto (norma española)

\defaultfontfeatures{Ligatures=TeX}

$if(mainfont)$
  \IfFontExistsTF{$mainfont$}{
    \setmainfont[Numbers=OldStyle]{$mainfont$}
  }{
    \setmainfont[Numbers=OldStyle]{TeX Gyre Pagella}
  }
$else$
  \setmainfont[Numbers=OldStyle]{TeX Gyre Pagella}
$endif$
$if(sansfont)$
  \IfFontExistsTF{$sansfont$}{
    \setsansfont[Scale=MatchLowercase]{$sansfont$}
  }{
    \setsansfont[Scale=MatchLowercase]{TeX Gyre Heros}
  }
$else$
  \setsansfont[Scale=MatchLowercase]{TeX Gyre Heros}
$endif$
$if(monofont)$
  \IfFontExistsTF{$monofont$}{
    \setmonofont[Scale=MatchLowercase]{$monofont$}
  }{
    \setmonofont[Scale=MatchLowercase]{TeX Gyre Cursor}
  }
$else$
  \setmonofont[Scale=MatchLowercase]{TeX Gyre Cursor}
$endif$

% ── Geometría ───────────────────────────────────────────────
\usepackage[$for(geometry)$$geometry$$sep$,$endfor$]{geometry}

% ── Colores institucionales FOC ─────────────────────────────
\usepackage{xcolor}
\definecolor{focblue}{HTML}{1F497D}    % azul base institucional
\definecolor{focmid}{HTML}{1F497D}     % unificado al azul base
\definecolor{foclight}{HTML}{BDD7EE}   % azul claro (filas alternas tablas)
\definecolor{focgray}{HTML}{595959}    % gris oscuro (header página)
\definecolor{focbg}{HTML}{F5F5F5}      % fondo bloques de código

% ── Interlineado ────────────────────────────────────────────
\usepackage{setspace}
$if(linestretch)$\setstretch{$linestretch$}$endif$
\setlength{\emergencystretch}{2em} % evita overfulls y cortes feos de línea
\clubpenalty=10000               % evita líneas huérfanas al inicio de página
\widowpenalty=10000              % evita líneas viudas al final de párrafo
\displaywidowpenalty=10000

% ── Microtipoografía ────────────────────────────────────────
\usepackage[final]{microtype}  % con XeLaTeX solo actúa sobre tracking

% ── Citas y comillas (buena práctica académica) ─────────────
\usepackage{csquotes}        % \enquote{} para citas tipográficamente correctas

% ── Matemáticas básicas (requerido por pandoc para tablas con \real{}) ──
\usepackage{calc}

% ── Tablas (estilo académico con booktabs) ──────────────────
\usepackage{booktabs}        % \toprule \midrule \bottomrule
\usepackage{longtable}       % tablas multipágina (pandoc las usa)
\usepackage{array}
\usepackage{tabularx}
\usepackage{colortbl}        % colores en celdas
\usepackage{etoolbox}        % hooks de entorno
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}

% Ajustes visuales de tablas académicas (legibilidad)
\definecolor{foctablerowodd}{HTML}{FFFFFF}
\definecolor{foctableroweven}{HTML}{EAF2FC}
\definecolor{foctableline}{HTML}{BFD0E3}

\setlength{\heavyrulewidth}{0.09em}
\setlength{\lightrulewidth}{0.05em}
\setlength{\cmidrulewidth}{0.04em}
\setlength{\aboverulesep}{0.45ex}
\setlength{\belowrulesep}{0.35ex}
\renewcommand{\arraystretch}{1.17}
\setlength{\tabcolsep}{5.5pt}

% Columnas con alineación y ancho definido
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}p{#1}}

% Estilo de tablas: helpers reutilizables para Markdown y LaTeX crudo
\newcommand{\focheadcell}[1]{\cellcolor{focmid}\color{white}\textbf{#1}}
\newcommand{\focheadrow}{\rowcolor{focblue}\color{white}\bfseries\sffamily}
\newcommand{\foctheadrow}{\focheadrow} % alias legado

\newcommand{\focapplytablestyle}{%
  \arrayrulecolor{foctableline}%
  \rowcolors{2}{foctableroweven}{foctablerowodd}%
}

\newcommand{\focresettablestyle}{%
  \rowcolors{2}{}{}%
  \arrayrulecolor{black}%
}

\AtBeginEnvironment{tabular}{\focapplytablestyle}%
\AtEndEnvironment{tabular}{\focresettablestyle}%
\AtBeginEnvironment{tabularx}{\focapplytablestyle}%
\AtEndEnvironment{tabularx}{\focresettablestyle}%
% longtable no usa zebra para evitar artefactos visuales (filas fantasma)

% ── Listas ──────────────────────────────────────────────────
\usepackage{enumitem}
\setlist[itemize,1]{label=\textcolor{focmid}{\textbullet}, leftmargin=1.8em, itemsep=0.2em}
\setlist[itemize,2]{label=\textcolor{focmid}{--},          leftmargin=1.5em, itemsep=0.1em}
\setlist[enumerate]{leftmargin=1.8em, itemsep=0.2em}

% ── Gráficos y figuras ──────────────────────────────────────
\usepackage{graphicx}
\usepackage{float}
\usepackage{pdflscape}
\usepackage{caption}
\captionsetup{
  font=small,
  labelfont={bf,color=focblue},
  textfont=it,
  labelsep=period,
}

% ── Código fuente ───────────────────────────────────────────
\usepackage{listings}
\lstset{
  basicstyle=\small\ttfamily,
  breaklines=true,
  breakatwhitespace=true,
  frame=single,
  rulecolor=\color{focmid},
  backgroundcolor=\color{focbg},
  commentstyle=\color{focgray}\itshape,
  keywordstyle=\color{focblue}\bfseries,
  stringstyle=\color{focmid},
  numbers=left,
  numberstyle=\tiny\color{focgray},
  numbersep=8pt,
  xleftmargin=12pt,
  showstringspaces=false,
}

% Bloques Shaded de pandoc (```código```)
\usepackage{framed}
\definecolor{shadecolor}{HTML}{F5F5F5}
\newenvironment{Shaded}{\begin{snugshade}}{\end{snugshade}}
\usepackage{fancyvrb}
\newcommand{\VerbBar}{|}
\newcommand{\VERB}{\Verb[commandchars=\\\{\}]}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{
  commandchars=\\\{\},
  fontsize=\small,
  frame=single,
  rulecolor=\color{focmid},
  fillcolor=\color{focbg},
}

% ── TikZ para portada ───────────────────────────────────────
\usepackage{tikz}
\usetikzlibrary{calc}

% ── Títulos de sección — KOMA-Script ────────────────────────
\usepackage{scrlayer-scrpage}  % encabezados KOMA
\usepackage{needspace}         % evita títulos al final de página sin contenido

% Personalización de títulos con scrreprt
\addtokomafont{chapter}{\color{focblue}\sffamily}
\addtokomafont{section}{\color{focmid}\sffamily}
\addtokomafont{subsection}{\color{focmid}\sffamily}
\addtokomafont{subsubsection}{\sffamily\bfseries}
\addtokomafont{chapterprefix}{\color{focblue}}

% Línea decorativa bajo cada chapter
\usepackage{titlesec}
\titleformat{\chapter}[block]
  {\color{focblue}\bfseries\Large\sffamily}
  {\thechapter.}{0.6em}{}
  [{\color{focblue}\vspace{2pt}\hrule height 0.5pt}]

\titlespacing*{\chapter}{0pt}{0.5em}{1.2em}

\titleformat{\section}[block]
  {\color{focmid}\bfseries\large\sffamily}
  {\thesection.}{0.5em}{}
\titlespacing*{\section}{0pt}{1.2em}{0.65em}

\titleformat{\subsection}[block]
  {\color{focmid}\bfseries\normalsize\sffamily}
  {\thesubsection.}{0.5em}{}
\titlespacing*{\subsection}{0pt}{0.95em}{0.45em}

\titleformat{\subsubsection}[block]
  {\bfseries\normalsize\sffamily}
  {}{0pt}{}
\titlespacing*{\subsubsection}{0pt}{0.8em}{0.35em}

% Evita encabezados "colgados" al pie de página (política más estricta)
\preto\section{\Needspace{12\baselineskip}}
\preto\subsection{\Needspace{10\baselineskip}}
\preto\subsubsection{\Needspace{7\baselineskip}}

% ── Encabezado y pie de página ──────────────────────────────
\clearpairofpagestyles
\ohead{\small\color{focgray}\sffamily FOMENTO OCUPACIONAL FOC \textregistered}
\ihead{}
\ofoot{\small\color{focblue}\bfseries\sffamily PÁGINA \pagemark}
\ifoot{\small\color{focgray}\sffamily $if(footer-text)$$footer-text$$else$$for(author)$$author$$sep$ -- $endfor$$endif$}
% También aplicar footer en páginas de estilo "plain" (inicio de capítulos, ToC, etc.)
\ofoot*{\small\color{focblue}\bfseries\sffamily PÁGINA \pagemark}
\ifoot*{\small\color{focgray}\sffamily $if(footer-text)$$footer-text$$else$$for(author)$$author$$sep$ -- $endfor$$endif$}
\KOMAoptions{headsepline=0.4pt}
\addtokomafont{headsepline}{\color{focgray}}
\pagestyle{scrheadings}

% Capítulos sin header pero sí footer
\renewcommand*{\chapterpagestyle}{plain.scrheadings}

% ── Índice ──────────────────────────────────────────────────
\usepackage{tocloft}
\renewcommand{\cfttoctitlefont}{\Large\bfseries\color{focblue}\sffamily}
\renewcommand{\cftchapfont}{\bfseries\color{focblue}\sffamily}
\renewcommand{\cftchappagefont}{\bfseries\color{focblue}\sffamily}
\renewcommand{\cftsecfont}{\color{focmid}\sffamily}
\renewcommand{\cftsecpagefont}{\color{focmid}\sffamily}
\renewcommand{\cftsubsecfont}{\sffamily}
\setlength{\cftbeforetoctitleskip}{0pt}

% ── Utilidades pandoc ───────────────────────────────────────
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% ── hyperref — SIEMPRE AL FINAL del preámbulo ───────────────
\usepackage{hyperref}
\hypersetup{
  colorlinks  = $if(colorlinks)$$colorlinks$$else$true$endif$,
  linkcolor   = $if(linkcolor)$$linkcolor$$else$focblue$endif$,
  urlcolor    = $if(urlcolor)$$urlcolor$$else$focmid$endif$,
  citecolor   = $if(citecolor)$$citecolor$$else$focmid$endif$,
}
\usepackage{bookmark}        % mejor manejo de bookmarks PDF (carga después de hyperref)

% ── PORTADA con TikZ ────────────────────────────────────────
\newcommand{\makefoccover}{%
  \begin{titlepage}
    \thispagestyle{empty}
    \newgeometry{margin=0pt}   % portada sin márgenes para control total

    \begin{tikzpicture}[remember picture, overlay]

      % Fondo blanco completo
      \fill[white] (current page.south west) rectangle (current page.north east);

      % ── Barra decorativa superior (azul institucional) ──
      \fill[focblue]
        (current page.north west) rectangle
        ($$(current page.north east)-(0,1.85cm)$$);

      % ── Marca institucional — logo + texto dentro de la barra azul ──
      \node[anchor=north west, inner sep=0pt]
        at ($$(current page.north west)+(0.95cm,-0.08cm)$$) {
          \includegraphics[height=2.4cm]{assets/foc-logo.png}
      };

      \node[anchor=north west, inner sep=0pt]
        at ($$(current page.north west)+(4.15cm,-0.08cm)$$) {
          \parbox[c][2.4cm][c]{8.8cm}{%
            \color{white}\bfseries\sffamily\fontsize{17}{17}\selectfont Instituto Fomento Ocupacional
          }
      };

      % ── Línea divisoria sutil ──
      \draw[focblue, line width=0.5pt]
        ($$(current page.north west)+(0,1.85cm)$$) --
        ($$(current page.north east)+(0,1.85cm)$$);

      % ── Título "VeriTix" — centrado,parte superior ──
      \node[anchor=center]
        at ($$(current page.center)+(0,3cm)$$) {
          \color{focblue}\fontsize{72}{76}\selectfont\bfseries\sffamily VeriTix
      };

      % ── Subtítulo — debajo del título ──
      \node[anchor=center, inner sep=0pt]
        at ($$(current page.center)+(0,0.5cm)$$) {
          \color{focblue}\fontsize{18}{20}\selectfont\sffamily
          Sistema de Gestión de Eventos y Tickets Digitales
      };

      % ── Línea horizontal decorativa ──
      \draw[focblue, line width=1pt]
        ($$(current page.center)+(-5cm, -1.5cm)$$) --
        ($$(current page.center)+(5cm, -1.5cm)$$);

      % ── Fase del proyecto ──
      \node[anchor=center, inner sep=0pt]
        at ($$(current page.center)+(0,-3cm)$$) {
          \color{focblue}\bfseries\sffamily\Large
          $if(phase)$$phase$$endif$
      };

      % ── Autores ──
      \node[anchor=center, inner sep=0pt]
        at ($$(current page.center)+(0,-5cm)$$) {
          \color{focblue}\sffamily\large
          $for(author)$$author$$sep$ ~~·~~ $endfor$
      };

      % ── Ciclo y fecha — esquina inferior derecha ──
      \node[anchor=south east, inner sep=0pt, align=right]
        at ($$(current page.south east)+(-2cm,2cm)$$) {
          \color{focblue}\bfseries\sffamily\large
          DAM/DAW\\[0.3em]
          \color{focblue}\sffamily\normalsize
          2026
      };

      % ── Footer institucional — esquina inferior izquierda ──
      \node[anchor=south west, inner sep=0pt]
        at ($$(current page.south west)+(2cm,2cm)$$) {
          \color{focblue}\sffamily\small
          Proyecto Fin de Ciclo
      };

    \end{tikzpicture}

    \restoregeometry
  \end{titlepage}
}

% ── Página de aviso legal ────────────────────────────────────
\newcommand{\makelegaldisclaimer}{%
  \thispagestyle{empty}
  \vspace*{\fill}
  \begin{small}\itshape
  Los documentos, elementos gráficos, vídeos, transparencias y otros recursos didácticos
  incluidos en este contenido pueden contener imprecisiones técnicas o errores tipográficos.
  Periódicamente se realizan cambios en el contenido. Fomento Ocupacional FOC SL puede
  realizar en cualquier momento, sin previo aviso, mejoras y/o cambios en el contenido.

  \vspace{0.8em}
  Es responsabilidad del usuario el cumplimiento de todas las leyes de derechos de autor
  aplicables. Ningún elemento de este contenido, ni parte de este contenido puede ser
  reproducida, almacenada o introducida en un sistema de recuperación, ni transmitida de
  ninguna forma ni por ningún medio, sin la previa autorización por escrito de Fomento
  Ocupacional FOC SL.

  \vspace{0.8em}
  Este contenido está protegido por la ley de propiedad intelectual e industrial. Pertenecen
  a Fomento Ocupacional FOC SL los derechos de autor y los demás derechos de propiedad
  intelectual e industrial sobre este contenido.

  \vspace{0.8em}
  Sin perjuicio de los casos en que la ley aplicable prohíbe la exclusión de la
  responsabilidad por daños, Fomento Ocupacional FOC SL no se responsabiliza en ningún
  caso de daños indirectos que se deriven del uso de este contenido.

  \vspace{0.8em}
  \textcopyright{} 2026 Fomento Ocupacional FOC SL todos los derechos reservados.
  \end{small}
  \vspace*{\fill}
  \newpage
}

% ════════════════════════════════════════════════════════════
%  DOCUMENTO
% ════════════════════════════════════════════════════════════
\begin{document}

\makefoccover
\makelegaldisclaimer

% Índice
{
  \hypersetup{linkcolor=focblue}
  \setcounter{tocdepth}{$if(toc-depth)$$toc-depth$$else$3$endif$}
  \tableofcontents
  \newpage
}

% Contenido generado por pandoc
$body$

\end{document}
